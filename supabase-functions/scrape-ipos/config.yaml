# Supabase Edge Function Configuration
# This tells Supabase to run the scraper automatically

# Function name
name: scrape-ipos

# Runtime
runtime: deno

# Import map for dependencies
import_map: ./import_map.json

# Environment variables (set these in Supabase dashboard)
# SUPABASE_URL: your project URL
# SUPABASE_SERVICE_ROLE_KEY: your service role key

# Cron schedule (runs automatically)
# This runs the scraper every day at midnight UTC
# You can adjust the schedule as needed
